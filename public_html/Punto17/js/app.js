alert(`Rule  of three is so: result = A * B / C`);
let a = parseInt(prompt('Value of A ? ')),
        b = parseInt(prompt('Value of B ? ')),
        c = parseInt(prompt('Value of C ? '));

alert(`${a * b / c}`);
