let width = parseInt(prompt("What is the width of the rectangle? ")),
        height = parseInt(prompt("What is the height of the rectangle? "));
alert(`Perimeter: ${2 * width + 2 * height}, cm
Area: ${width * height}, cm`);

